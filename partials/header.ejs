<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShipIt | <%=title %> </title>  
      <link rel="icon" type="image/x-icon" href="/assets/ship (1).svg">
      <style>
        body{
          font-family: Arial;
        }
        #loading {
        position: fixed;
        top: 0;
        left: 0;
        padding:45vh 48vw;
        width: 100%;
        height: 100%;
        background-color: rgba(255, 255, 255, 1); /* Semi-transparent background */
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 9999; /* Ensure it's above other content */
     }

     #loading img {
      width: 50px; /* Adjust size as needed */
      height: 50px;
     }
     #logout{
      display:none;
     }
        #footer{
          background-color: rgb(4, 4, 49);
          color:white;
          text-align: center;
          border-top-left-radius:40px;
          border-top-right-radius:40px;
          padding-top:1.5%;
          padding-bottom:1%;    
          margin-bottom:-1vh;
          /* here % means with respective to the width of the parent element */
        }
      </style>